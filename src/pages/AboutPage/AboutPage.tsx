import { FC } from "react"
import styles from "./AboutPage.module.scss"

const AboutPage: FC = () => {
    return (
        <div>
            About Page
        </div>
    );
};

export default AboutPage;